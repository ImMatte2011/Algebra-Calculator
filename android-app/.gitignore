# Gradle
.gradle/
/build/
app/build/

# Local configuration (contains SDK path and machine-specific paths — never commit)
local.properties

# Signing keystores and passwords — NEVER commit these
*.jks
*.keystore
keystore.properties

# IDE — IntelliJ / Android Studio
.idea/
*.iml

# macOS
.DS_Store

# Windows
Thumbs.db

# Kotlin compiler sessions
.kotlin/
gradle/wrapper/gradle-wrapper.jar
