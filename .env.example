# Server .env example
API_HOST=127.0.0.1
API_PORT=8000
API_TOKEN=replace_with_secure_token

# ACCESS_MODE: "public" (Caddy + Internet, token enforced) or
#              "tailscale" (only reachable via Tailscale, token skipped).
# Default if unset: public.
ACCESS_MODE=public

LOG_LEVEL=INFO
